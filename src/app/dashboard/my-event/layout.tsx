// This section has moved to /members (matching the legacy PHP route names). These routes are
// kept only as redirect stubs so old links/bookmarks still land somewhere useful.
export default function MyEventLayout({ children }: { children: React.ReactNode }) {
  return children;
}
